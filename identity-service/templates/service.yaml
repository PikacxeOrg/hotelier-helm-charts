apiVersion: v1
kind: Service
metadata:
  name: { { include "identity-service.fullname" . } }
  labels:
    app: { { include "identity-service.name" . } }
spec:
  type: { { .Values.service.type } }
  selector:
    app: { { include "identity-service.name" . } }
  ports:
    - name: http
      port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
