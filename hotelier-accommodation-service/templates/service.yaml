apiVersion: v1
kind: Service
metadata:
  name: { { include "accommodation-service.fullname" . } }
  labels:
    app: { { include "accommodation-service.name" . } }
spec:
  type: { { .Values.service.type } }
  selector:
    app: { { include "accommodation-service.name" . } }
  ports:
    - name: http
      port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
