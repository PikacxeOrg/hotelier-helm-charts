apiVersion: v1
kind: Service
metadata:
  name: { { include "availability-service.fullname" . } }
  labels:
    app: { { include "availability-service.name" . } }
spec:
  type: { { .Values.service.type } }
  selector:
    app: { { include "availability-service.name" . } }
  ports:
    - name: http
      port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
